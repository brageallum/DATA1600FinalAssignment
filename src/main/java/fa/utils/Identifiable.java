package fa.utils;

/**
 * Every class that implements this interface should give all objects a unique ID,
 * which is accessible through the getID method.
 */
public interface Identifiable {
  int getID();
}
